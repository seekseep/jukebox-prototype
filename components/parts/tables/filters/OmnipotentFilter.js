import DefaultFilter from './DefaultFilter'

// TODO: to be omnipotent
export default function OmnipotentFilter ({ instance, column }) {
  return <DefaultFilter instance={instance} column={column} />
}
